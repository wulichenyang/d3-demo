import React from 'react'
import styled from 'styled-components'

interface IProps {
  loading: boolean
}

const Loader:React.FC<IProps> = ({ loading }: { loading: boolean }) => {
  return  (<div></div>)
}

export default Loader